File_Create empty file_"nume.txt"

#include <stdio.h>
#include <stdlib.h>

int main()
{
FILE *fp;
fp = fopen("myfile.txt", "r");
int ch;
while((ch = fgetc(fp))!= EOF)
printf("%c", ch);
    return 0;
}
